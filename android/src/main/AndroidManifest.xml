<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <application>
    <!-- Auto-init: runs before Application.onCreate, no MainActivity edits needed. -->
    <provider
      android:name="com.margelo.nitro.splash.SplashScreenInitProvider"
      android:authorities="${applicationId}.osukisplash"
      android:exported="false"
      android:initOrder="900" />
  </application>
</manifest>
