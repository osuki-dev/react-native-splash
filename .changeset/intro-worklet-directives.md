---
'@osuki-dev/react-native-splash': patch
---

fix(intros): add explicit 'worklet' directives to all intro animation callbacks and animated styles to prevent UI Runtime remote function crashes (#11)
